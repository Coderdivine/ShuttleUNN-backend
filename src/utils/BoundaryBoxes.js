class BoundaryBoxes {
    
    constructor() {
        return true;
    }

    // Returns values of body pose with degrees
    async getBody() {} 

    // Returns values of hand pose with degrees
    async getHand() {}

    // Returns values of pose with degrees
    async joinParts() {}
}

module.exports = new BoundaryBoxes();